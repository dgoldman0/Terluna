# Checkpoint browser harnesses

The capture and check scripts used to verify the shoreline checkpoints (M1,
landscape, r186, revision 04). They target the old self-contained HTML build
(`Open_Moon_Shoreline.html`), so they document how each checkpoint's validation
records were produced. They will not run against the current Vite build without
changes to how they load the page.
